fn main() {
    items = ["a", "b", "c"];
    s: str = "";

    // for-of (colon): iterate values
    for (v: items) {
        s = s + v;
    }
    print(s);
    print("|");

    // for-in (in): iterate indices
    s = "";
    for (i in items) {
        s = s + str(i);
    }
    print(s);
}
