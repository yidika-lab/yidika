use {re} from "re";
use {json} from "json";
use {dt} from "datetime";
use {p} from "path";
fn main() {
    parsed: auto = json.parse("{\"name\":\"Yidika\",\"ver\":1}");
    print(stringify(parsed));
    print("Now: " + dt.now());
    ts: int = dt.timestamp();
    print("Year: " + str(dt.year(ts)));
    print("Formatted: " + dt.format(ts, "%Y-%m-%d %H:%M:%S"));
    print("Join: " + p.join("a", "b", "c.txt"));
}
