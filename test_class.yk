class Animal {
    name: str;
    sound: str;

    fn speak(self) -> str {
        return self.name + " says " + self.sound;
    }
}

fn main() {
    a = Animal { name: "Rex", sound: "woof" };
    b = Animal{name:23, sound:true};
    print(b);
    print(a.speak());
    print(a.name);
}
